package org.jumpmind.pos.test.params;

import static lombok.AccessLevel.PRIVATE;

import lombok.NoArgsConstructor;

/**
 * A set of JUnit argument providers which dispense randomized simple data elements.
 *
 * @author Jason Weiss
 */
@NoArgsConstructor(access = PRIVATE)
public class ArgumentProviders {
    // TODO Add junit-jupiter-params as a dependency so we can create @ArgumentsSources here.
}
